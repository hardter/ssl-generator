---
local_ca_owner: "{{ owner }}"
local_ca_group: "{{ group }}"
local_ca_base_path: "{{ base_path }}"
local_ca_config: "{{ base_path }}/{{ config }}"
local_ca_key: "{{ base_path }}/{{ key }}"
local_ca_cert: "{{ base_path }}/{{ cert }}"
local_ca_output_dir: "{{ base_path }}{{ output_dir }}"
local_ca_private_dir: "{{ base_path }}{{ private_dir }}"
local_ca_certs_dir: "{{ base_path }}{{ certs_dir }}"
local_ca_crl_dir: "{{ base_path }}{{ crl_dir }}"
local_ca_newcerts_dir: "{{ base_path }}{{ newcerts_dir }}"
local_ca_requests_dir: "{{ base_path }}{{ requests_dir }}"
local_ca_server_name: "{{ server_name }}"
local_ca_server_fqdn: "{{ server_fqdn }}"
local_ca_passphrase: "{{ passphrase }}"
local_ca_country: "{{ country }}"
local_ca_state: "{{ state }}"
local_ca_town: "{{ town }}"
local_ca_org: "{{ org }}"
local_ca_contact_email: "{{ contact_email }}"
local_ca_is_windows: "{{ is_windows }}"